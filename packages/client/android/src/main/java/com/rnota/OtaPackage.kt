package com.rnota

/**
 * ReactPackage placeholder for MS0.
 * Real TurboModule registration lands in later milestones (M9 bridge).
 *
 * NOTE: This class does not implement ReactPackage yet so the Android
 * library compiles without a hard dependency on a specific RN version
 * during the scaffold phase. Wire ReactPackage in MS1+/MS5 when linking
 * against a concrete RN binary in the example app.
 */
class OtaPackage
