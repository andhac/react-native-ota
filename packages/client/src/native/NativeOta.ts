/**
 * TurboModule spec placeholder (codegen reserved).
 *
 * MS0: no native methods. MS2 adds notifyAppReady; MS5 expands the contract.
 * Changes must remain additive-only once shipped (JS↔native skew).
 */

export type NativeOtaSpec = {
  readonly _placeholder?: true;
};

export const NATIVE_MODULE_NAME = 'RNOta' as const;
